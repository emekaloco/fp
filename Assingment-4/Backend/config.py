class Config:
    DEBUG = True  # Enable debugging for development
    JSON_SORT_KEYS = False  # Prevent JSON keys from being sorted
    SECRET_KEY = "your-secret-key"  # Replace with a secure random key